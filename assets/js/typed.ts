import Typed from "typed.js";

var options = {
  strings: ["<i>Discover</i> art &amp; pain", "on this unique planet"],
  typeSpeed: 80,
  loop: true,
};

var typed = new Typed(".typed", options);
//typed.start();
